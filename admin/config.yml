# config.yml

backend:
  name: git-gateway
  branch: master # Branch to update (optional; defaults to master)
media_folder: 'assets/images/uploads'

collections:
  - name: 'media'
    label: 'Media'
    folder: '_media/'
    fields:
      - { name: Title }